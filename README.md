# 🐔 Chicken Road Landing

Продающий одностраничник для шаблонного интерактивного продукта Chicken Road с модальным конструктором и интеграцией мессенджеров.

## 🚀 Быстрый старт

```bash
# Установка зависимостей
npm install

# Запуск dev сервера
npm run dev

# Сборка для production
npm run build

# Предварительный просмотр production сборки
npm run preview
```

## 📂 Структура проекта

```
Website/
├── public/              # Статичные файлы (модели, текстуры)
├── src/
│   ├── main.ts          # Основная логика
│   ├── style.css        # Глобальные стили
│   └── components/      # Компоненты (будут добавлены)
├── index.html           # HTML разметка
├── vite.config.ts       # Конфигурация Vite
├── netlify.toml         # Конфигурация Netlify
└── DEVELOPMENT_PLAN.md  # План разработки
```

## 🎨 Дизайн-система

### Цвета
- Primary: `#FFC107` (желтый), `#FF9800` (оранжевый), `#FF5722` (красный)
- Accents: `#4CAF50` (зеленый), `#2196F3` (синий)
- Backgrounds: `#1a1a1a` (темный), `#ffffff` (светлый)

### Типографика
- Заголовки: **Fredoka** (игровой шрифт)
- Текст: **Nunito** (читаемый sans-serif)
- Все размеры адаптивные через `clamp()`

### Адаптивность
Все элементы используют относительные единицы (`vw`, `vh`, `rem`, `em`) и функцию `clamp()` для плавной адаптации под любые экраны.

## 🛠 Технологии

- **Vite 5.x** - сборщик и dev сервер
- **TypeScript** - типизация
- **Three.js** - 3D превью в конструкторе
- **Pure CSS** - стилизация (без фреймворков)
- **Netlify** - хостинг и деплой

## 📋 План разработки

См. подробный план в файле `DEVELOPMENT_PLAN.md`

### Прогресс

- ✅ Настройка проекта
- ✅ Шапка и навигация
- ✅ Глобальные стили
- ✅ Hero секция
- ⏳ Секции контента (Features, Gallery, Pricing, FAQ)
- ⏳ Модальный конструктор
- ⏳ Интеграция мессенджеров
- ⏳ Анимации и полировка

## 📱 Браузеры

Поддерживаются все современные браузеры:
- Chrome/Edge 90+
- Firefox 88+
- Safari 14+
- Mobile браузеры (iOS Safari, Chrome Mobile)

## 🔗 Деплой на Netlify

```bash
# Подключите репозиторий к Netlify
# Настройки уже прописаны в netlify.toml

# Или используйте Netlify CLI
npm install -g netlify-cli
netlify deploy --prod
```

## 📊 Аналитика

События для отслеживания:
- `hero_cta_click` - клик на главную CTA
- `configurator_open` - открытие конструктора
- `whatsapp_send` / `telegram_send` - отправка в мессенджер
- `pricing_view` - просмотр тарифов
- `faq_expand` - раскрытие FAQ

## 📄 Лицензия

Proprietary - все права защищены


