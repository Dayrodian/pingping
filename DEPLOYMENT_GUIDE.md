# 🚀 Руководство по деплою Chicken Road Landing

## 📦 Как это работает

### Структура после сборки:

```
dist/                           ← Netlify публикует эту папку
├── index.html                  ← Лендинг (главная страница)
├── assets/                     ← JS/CSS лендинга
│   ├── index-abc123.js
│   └── index-xyz789.css
├── game/                       ← Ваша игра (копируется as-is)
│   ├── index.html              ← HTML игры
│   ├── main.js                 ← Логика игры
│   └── assets/                 ← Ассеты игры
└── images/                     ← Картинки
```

### URL структура:

- **Лендинг**: `https://your-site.netlify.app/`
- **Игра**: `https://your-site.netlify.app/game/`

---

## 🎮 Интеграция вашей игры

### Шаг 1: Подготовьте игру

Скопируйте все файлы вашей игры Chicken Road в папку `public/game/`:

```bash
Website/public/game/
├── index.html       ← Главный файл игры
├── main.js          ← Логика
├── style.css        ← Стили
└── assets/          ← Все ассеты
    ├── models/
    ├── textures/
    └── sounds/
```

### Шаг 2: Добавьте код для CTA

В вашу игру (`public/game/index.html` или в JS файл) добавьте:

```javascript
// Функция для отправки события родительскому окну
function sendToParent(type, data = {}) {
  window.parent.postMessage({
    type,
    ...data
  }, '*');
}

// Вызовите когда нужно показать CTA:

// Вариант 1: Через определенное время (20 секунд)
setTimeout(() => {
  sendToParent('SHOW_CTA', {
    score: currentScore,
    time: gameTime,
    level: currentLevel
  });
}, 20000);

// Вариант 2: При определенном событии
if (score >= 100) {
  sendToParent('SHOW_CTA', {
    score: score,
    time: gameTime
  });
}

// Вариант 3: При game over
function onGameOver() {
  sendToParent('SHOW_CTA', {
    score: finalScore,
    time: totalTime
  });
}
```

### Шаг 3: Тестирование локально

```bash
# Запустите dev сервер
npm run dev

# Откройте браузер
http://localhost:3000

# Игра будет встроена в hero секцию
# CTA модалка появится автоматически при событии
```

### Доступные события:

```javascript
// Старт игры
sendToParent('GAME_START');

// Game Over
sendToParent('GAME_OVER', { score: 100 });

// Победа
sendToParent('GAME_WIN', { score: 500 });

// Показать CTA
sendToParent('SHOW_CTA', { 
  score: 150, 
  time: 30,    // секунды
  level: 5 
});
```

---

## 🔧 Сборка и деплой

### 1. Соберите проект

```bash
npm run build
```

Это создаст папку `dist/` со всем необходимым.

### 2. Проверьте локально

```bash
npm run preview
```

Откроется `http://localhost:4173` с production версией.

### 3. Деплой на Netlify

#### Вариант A: Через Netlify CLI

```bash
# Установите CLI (один раз)
npm install -g netlify-cli

# Логин
netlify login

# Деплой
netlify deploy --prod
```

#### Вариант B: Через Git + Netlify

1. Сделайте git репозиторий:
```bash
git init
git add .
git commit -m "Initial commit"
git remote add origin YOUR_REPO_URL
git push -u origin main
```

2. Подключите в Netlify:
   - Зайдите на netlify.com
   - New site from Git
   - Выберите репозиторий
   - Build command: `npm run build`
   - Publish directory: `dist`
   - Деплой!

#### Вариант C: Drag & Drop

1. Зайдите на netlify.com
2. Перетащите папку `dist/` в окно
3. Готово!

---

## ⚙️ Настройки Netlify

Файл `netlify.toml` уже настроен:

```toml
[build]
  command = "npm run build"
  publish = "dist"

[[redirects]]
  from = "/*"
  to = "/index.html"
  status = 200
```

Это обеспечивает:
- ✅ Правильную сборку
- ✅ Работу Single Page Application
- ✅ Корректные URL для игры

---

## 🧪 Тестирование после деплоя

### 1. Проверьте лендинг
- Откройте `https://your-site.netlify.app/`
- Должна загрузиться главная страница
- Игра должна быть встроена в hero блок

### 2. Проверьте игру отдельно
- Откройте `https://your-site.netlify.app/game/`
- Игра должна работать standalone

### 3. Проверьте CTA
- Поиграйте 15-20 секунд или достигните нужного score
- Должна появиться модалка с CTA
- Проверьте кнопки "Продолжить" и "Сделать заказ"

### 4. Проверьте на мобильных
- Откройте на телефоне
- Проверьте адаптивность
- Убедитесь что игра работает в iframe

---

## 🐛 Troubleshooting

### Игра не загружается

**Проблема:** Iframe показывает пустую страницу

**Решение:**
1. Проверьте что файл `public/game/index.html` существует
2. Проверьте консоль браузера (F12) на ошибки
3. Убедитесь что пути к ассетам в игре относительные, а не абсолютные

### CTA не показывается

**Проблема:** Модалка не появляется

**Решение:**
1. Откройте консоль (F12)
2. Должны быть логи: `🎮 Game started` и `💬 CTA modal shown`
3. Проверьте что в игре вызывается `sendToParent('SHOW_CTA')`

### Ошибки CORS

**Проблема:** Блокировка cross-origin

**Решение:**
- Это нормально в dev режиме
- На production (Netlify) всё работает т.к. домен один

### Игра тормозит в iframe

**Проблема:** Низкий FPS

**Решение:**
1. Уменьшите качество графики для встроенной версии
2. Оптимизируйте ассеты (сжатие текстур)
3. Отключите тяжелые эффекты в iframe версии

---

## 📊 Аналитика

Все события автоматически логируются:

```javascript
// В консоли видны:
trackEvent('Game', 'start', 'play');
trackEvent('CTA', 'show', 'game-modal');
trackEvent('CTA', 'order', 'from-game');
```

Позже можно подключить:
- Google Analytics 4
- Yandex Metrica
- Facebook Pixel

---

## 🔄 Обновление игры

Чтобы обновить только игру:

1. Замените файлы в `public/game/`
2. Пересоберите: `npm run build`
3. Задеплойте: `netlify deploy --prod`

Лендинг останется без изменений, обновится только игра!

---

## ✅ Чеклист перед релизом

- [ ] Игра работает локально (`npm run dev`)
- [ ] CTA модалка появляется
- [ ] Кнопки работают
- [ ] Адаптив на мобильных
- [ ] Production build проверен (`npm run preview`)
- [ ] Нет ошибок в консоли
- [ ] Все ассеты загружаются
- [ ] Аналитика настроена
- [ ] Netlify деплой успешен
- [ ] Протестировано на разных устройствах

---

Готово! 🚀 Ваш лендинг с интегрированной игрой работает!


