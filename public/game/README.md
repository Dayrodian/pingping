# 🎮 Chicken Road Game Integration

## Структура

Поместите сюда вашу готовую игру Chicken Road:

```
game/
├── index.html       ← Главный файл игры
├── main.js          ← Логика игры
├── style.css        ← Стили игры
└── assets/          ← Ассеты (модели, текстуры, звуки)
    ├── models/
    ├── textures/
    └── sounds/
```

## Интеграция с лендингом

### 1. Добавьте код для показа CTA

В вашей игре добавьте функцию, которая отправляет сообщение родительскому окну:

```javascript
// В файле логики игры
function triggerCTA() {
  window.parent.postMessage({
    type: 'SHOW_CTA',
    score: gameScore,        // Текущий счет игрока
    time: gameTime,          // Время игры
    level: currentLevel      // Уровень
  }, '*');
}

// Вызовите эту функцию когда нужно:
// - Через 20 секунд игры
// - После прохождения 3 уровней
// - При достижении определенного счета

// Пример: через 20 секунд
setTimeout(triggerCTA, 20000);

// Или при определенном событии
if (score >= 100) {
  triggerCTA();
}
```

### 2. Отключите стандартные CTA игры (если есть)

Если в игре уже есть кнопки "Play Now" или другие CTA, скройте их:

```javascript
// Скрываем стандартные CTA
const originalCTA = document.querySelector('.original-cta-button');
if (originalCTA) {
  originalCTA.style.display = 'none';
}
```

### 3. Тестирование

Откройте игру напрямую:
- Локально: http://localhost:3000/game/
- На сервере: https://ваш-сайт.netlify.app/game/

Лендинг автоматически перехватит событие и покажет модалку.

## События для аналитики

Игра может отправлять различные события:

```javascript
// Начало игры
window.parent.postMessage({ type: 'GAME_START' }, '*');

// Игрок проиграл
window.parent.postMessage({ type: 'GAME_OVER', score: finalScore }, '*');

// Игрок выиграл
window.parent.postMessage({ type: 'GAME_WIN', score: finalScore }, '*');

// Показать CTA
window.parent.postMessage({ type: 'SHOW_CTA', score: currentScore }, '*');
```

Все события автоматически логируются в аналитику.

## Важно

⚠️ Не забудьте протестировать игру перед деплоем:
1. Работает ли в iframe
2. Отправляются ли события
3. Нет ли конфликтов стилей
4. Работает ли на мобильных


